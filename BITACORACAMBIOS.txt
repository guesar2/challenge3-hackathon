run_n_scaling.py (new) — drop into your project root. Runs the system-size sweep.
pauli_ops.py (replace) — the important fix. get_pauli used dense np.kron; at N=20 that's a 17.6 TB matrix. Rewrote it with scipy.sparse.kron throughout, so build_tfim_hamiltonian and build_collective_observables never materialize a dense 2^N×2^N array.
plotting.py (replace) — added plot_n_scaling().
config.py (replace) — added the N_SCALING_* knobs.
ftim_main.py (replace) — added run_n_scaling as stage 5/6.
run_ed.py — now prints the ED baseline table for config.N (6) and then for every N in config.ED_EXTRA_N_VALUES. Ran it above: both N=6 and N=8 tables print in the same format, back to back.
config.py — added ED_EXTRA_N_VALUES = (8,).



Importante, le dejé un stockholder para meterle la parte de ruido después