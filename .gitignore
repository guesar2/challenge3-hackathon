# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
ENV/
env/
.venv/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Data outputs — keep only the canonical *_latest.json pointer per stage,
# ignore timestamped snapshots and other generated artifacts.
data/*.json
!data/*_latest.json

# Figures generated when running scripts from inside src/ instead of the
# repo root (python main.py writes to figures/ at the repo root instead)
src/figures/

# Quantinuum credentials (NEVER commit)
*.token
*.key
.env

# Claude
CLAUDE.md
docs/hackathon.pdf
